<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="/output.css" rel="stylesheet" />
  <title><%= home.housename %> - Home Details</title>
</head>
<body class="bg-gray-50 text-gray-800 font-sans">

  <!-- ✅ NAVBAR -->
  <nav class="bg-white shadow-md">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <a href="/" class="text-2xl font-bold text-orange-600 hover:text-orange-700 transition">AirbnbClone</a>
        <div class="flex space-x-6 text-lg">
          <a href="/host/add-home" class="text-gray-700 hover:text-orange-600 font-medium">➕ Add Home</a>
          <a href="/bookings" class="text-gray-700 hover:text-orange-600 font-medium">📑 Bookings</a>
          <a href="/favourite" class="text-gray-700 hover:text-orange-600 font-medium">❤️ Favourite List</a>
          <a href="/reserve" class="text-gray-700 hover:text-orange-600 font-medium">📅 Reserve</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- ✅ Home Details Section -->
  <main class="max-w-5xl mx-auto p-6 mt-8 bg-white rounded-xl shadow-md">
    <h1 class="text-3xl font-bold text-orange-600 mb-4 text-center">
      🏡 <%= home.housename %>
    </h1>

    <!-- Image -->
    <div class="mb-6">
      <img src="<%= home.photo || 'https://via.placeholder.com/800x400?text=No+Image' %>"
           alt="Home Image"
           class="w-full h-64 object-cover rounded-lg shadow" />
    </div>

    <!-- Info -->
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 text-lg">
      <p><strong>📍 Location:</strong> <%= home.location || 'Not available' %></p>
      <p><strong>💰 Price:</strong> ₹<%= home.price || 'N/A' %>/night</p>
      <p><strong>⭐ Rating:</strong> <%= home.rating || '4.5' %></p>
      <p><strong>🆔 ID:</strong> <%= home._id %></p>
    </div>

    <!-- Description -->
    <div class="mt-6 text-gray-700">
      <h2 class="text-xl font-semibold mb-2">📝 Description</h2>
      <p>
        Welcome to <%= home.housename %>, a beautiful getaway located in the heart of <%= home.location || 'your chosen city' %>.
        Enjoy premium comfort and a relaxing atmosphere with a host of amenities designed to make your stay unforgettable.
      </p>
    </div>

    <!-- Actions -->
    <div class="mt-8 flex justify-center gap-4">
      <a href="/book/<%= home._id %>"
         class="bg-orange-500 hover:bg-orange-600 text-white font-semibold px-6 py-3 rounded-lg transition shadow">
        🛏️ Book Now
      </a>
      <a href="/" class="bg-white border-2 border-orange-500 text-orange-600 font-semibold px-6 py-3 rounded-lg hover:bg-orange-50 transition">
        🔙 Back to Home
      </a>
    </div>
  </main>

</body>
</html>
